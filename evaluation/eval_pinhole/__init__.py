"""Pinhole depth evaluation on the WAI test set (eth3d / scannetppv2) + OmniOCC.

Entry point:
    run_eval.py - covisibility-sample views per scene, run the X-Lens model, and
                  report per-view depth metrics aggregated per dataset + overall.
"""
